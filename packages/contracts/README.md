# Contrats partagés

`openapi.yaml` est la source commune pour le portail, les sites d'établissement et le futur dashboard. Toute rupture de contrat devra être versionnée (`/v2`, par exemple) afin que chaque application puisse être publiée indépendamment.
